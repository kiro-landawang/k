<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="description" content="ANGEL官网：工具、服务与社群入口。">
  <meta name="theme-color" content="#090b16">
  <meta name="author" content="ANGEL">
  <meta name="keywords" content="ANGEL,资源导航,工具合集,卡网,Telegram,QQ群">
  <meta property="og:type" content="website">
  <meta property="og:title" content="ANGEL官网">
  <meta property="og:description" content="工具、服务与社群入口。">
  <meta property="og:url" content="https://yesir123886.github.io/k/">
  <meta property="og:image" content="assets/logo.png">
  <meta name="twitter:card" content="summary">
  <meta name="twitter:title" content="ANGEL官网">
  <meta name="twitter:description" content="工具、服务与社群入口。">
  <meta name="twitter:image" content="assets/logo.png">
  <link rel="icon" type="image/png" href="assets/favicon.png">
  <link rel="icon" type="image/png" href="assets/logo.png">
  <link rel="apple-touch-icon" href="assets/logo.png">
  <link rel="manifest" href='data:application/json,{"name":"ANGEL官网","short_name":"ANGEL","start_url":"/","display":"standalone","background_color":"#090b16","theme_color":"#090b16","icons":[{"src":"assets/logo.png","sizes":"512x512","type":"image/png"}]}'>
  <link rel="preconnect" href="https://t.me">
  <link rel="preconnect" href="https://qm.qq.com">
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "ANGEL官网",
    "url": "https://yesir123886.github.io/k/",
    "description": "工具、服务与社群入口。",
    "publisher": { "@type": "Organization", "name": "ANGEL" }
  }
  </script>
  <title>ANGEL官网</title>
  <style>
    :root {
      color-scheme: dark;
      --bg: #080a13;
      --surface: rgba(18, 21, 38, .86);
      --surface-strong: #15182a;
      --surface-soft: rgba(255, 255, 255, .055);
      --line: rgba(255, 255, 255, .11);
      --line-strong: rgba(255, 255, 255, .19);
      --text: #f6f7ff;
      --muted: #b5b9cf;
      --subtle: #898fa9;
      --purple: #8b7cff;
      --purple-2: #c084fc;
      --cyan: #56e6c1;
      --pink: #ff7ab8;
      --amber: #ffd166;
      --danger: #ff7b87;
      --radius-xl: 28px;
      --radius-lg: 20px;
      --radius-md: 15px;
      --shadow: 0 24px 80px rgba(0, 0, 0, .36);
      --max: 1180px;
    }

    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; scroll-padding-top: 92px; }
    body {
      margin: 0;
      min-height: 100vh;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", "Microsoft YaHei", sans-serif;
      color: var(--text);
      background:
        radial-gradient(circle at 15% 12%, rgba(139, 124, 255, .16), transparent 40%),
        var(--bg);
      overflow-x: hidden;
    }

    body::before {
      content: "";
      position: fixed;
      inset: 0;
      pointer-events: none;
      opacity: .25;
      background-image:
        linear-gradient(rgba(255,255,255,.025) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,.025) 1px, transparent 1px);
      background-size: 44px 44px;
      mask-image: linear-gradient(to bottom, black, transparent 82%);
      z-index: -1;
    }

    a { color: inherit; }
    button, a { -webkit-tap-highlight-color: transparent; }
    button { font: inherit; }
    img { display: block; max-width: 100%; }
    :focus-visible { outline: 3px solid var(--cyan); outline-offset: 3px; }
    .skip-link { position: fixed; left: 12px; top: -80px; z-index: 100; padding: 10px 14px; border-radius: 10px; background: white; color: #111; }
    .skip-link:focus { top: 12px; }

    .ambient-video {
      position: fixed;
      inset: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      opacity: 0;
      transition: opacity .5s ease;
      pointer-events: none;
      z-index: -3;
      filter: saturate(.78) brightness(.38);
    }
    body.video-on .ambient-video { opacity: .42; }
    body.video-on::before { opacity: .15; }

    .nav-shell {
      position: sticky;
      top: 0;
      z-index: 20;
      padding: 14px 18px 0;
    }
    .navbar {
      max-width: var(--max);
      min-height: 62px;
      margin: 0 auto;
      padding: 10px 12px 10px 18px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      border: 1px solid var(--line);
      border-radius: 18px;
      background: rgba(10, 12, 24, .78);
      box-shadow: 0 10px 38px rgba(0, 0, 0, .25);
    }
    .brand { display: inline-flex; align-items: center; gap: 11px; text-decoration: none; font-weight: 850; letter-spacing: .08em; }
    .brand-mark {
      width: 36px;
      height: 36px;
      border-radius: 11px;
      object-fit: cover;
      border: 1px solid rgba(255,255,255,.15);
      background: linear-gradient(135deg, var(--purple), var(--cyan));
      box-shadow: 0 0 24px rgba(139,124,255,.23);
    }
    .nav-links { display: flex; align-items: center; gap: 5px; }
    .nav-links a, .icon-btn {
      min-height: 40px;
      padding: 0 13px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 7px;
      border: 1px solid transparent;
      border-radius: 12px;
      color: var(--muted);
      background: transparent;
      text-decoration: none;
      cursor: pointer;
      transition: color .2s ease, background-color .2s ease, border-color .2s ease;
    }
    .nav-links a:hover, .icon-btn:hover { color: white; background: var(--surface-soft); border-color: var(--line); }
    .nav-actions { display: flex; align-items: center; gap: 6px; }
    .icon-btn { width: 40px; padding: 0; }
    .menu-btn { display: none; }

    main { max-width: var(--max); margin: 0 auto; padding: 48px 18px 72px; }
    .hero {
      display: grid;
      grid-template-columns: minmax(0, 1.35fr) minmax(300px, .65fr);
      gap: 22px;
      align-items: stretch;
      min-height: 480px;
    }
    .hero-main, .profile-card, .section-card {
      border: 1px solid var(--line);
      background: var(--surface);
      box-shadow: var(--shadow);
    }
    .hero-main {
      position: relative;
      overflow: hidden;
      padding: clamp(32px, 6vw, 72px);
      border-radius: var(--radius-xl);
      display: flex;
      flex-direction: column;
      justify-content: center;
    }
    .hero-main::after {
      content: "A";
      position: absolute;
      right: -22px;
      bottom: -112px;
      font-size: clamp(300px, 42vw, 540px);
      line-height: 1;
      font-weight: 950;
      color: transparent;
      -webkit-text-stroke: 1px rgba(255,255,255,.06);
      pointer-events: none;
    }
    .eyebrow { display: flex; align-items: center; gap: 9px; width: fit-content; margin-bottom: 22px; color: var(--cyan); font-size: 13px; font-weight: 760; letter-spacing: .12em; text-transform: uppercase; }
    .eyebrow::before { content: ""; width: 25px; height: 1px; background: currentColor; }
    h1 { position: relative; z-index: 1; margin: 0; max-width: 760px; font-size: clamp(54px, 9vw, 108px); line-height: .92; letter-spacing: -.065em; }
    .gradient-text { background: linear-gradient(115deg, white 15%, #d7d0ff 43%, var(--purple) 70%, var(--cyan)); background-clip: text; -webkit-background-clip: text; color: transparent; }
    .lead { position: relative; z-index: 1; max-width: 650px; margin: 24px 0 0; color: var(--muted); font-size: clamp(17px, 2vw, 21px); line-height: 1.75; }
    .hero-actions { position: relative; z-index: 1; margin-top: 34px; display: flex; flex-wrap: wrap; gap: 12px; }
    .btn {
      min-height: 50px;
      padding: 0 19px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 9px;
      border: 1px solid var(--line);
      border-radius: 14px;
      color: white;
      background: rgba(255,255,255,.06);
      font-weight: 720;
      text-decoration: none;
      cursor: pointer;
      transition: border-color .15s ease, background-color .15s ease;
    }
    .btn:hover { transform: translateY(-2px); border-color: var(--line-strong); background: rgba(255,255,255,.09); }
    .btn-primary { color: #0a0b12; border-color: transparent; background: linear-gradient(135deg, #a99dff, #69efd0); box-shadow: 0 10px 32px rgba(115,222,202,.18); }
    .btn-primary:hover { background: linear-gradient(135deg, #b5abff, #7cf5d8); box-shadow: 0 14px 38px rgba(115,222,202,.27); }

    .profile-card { padding: 26px; border-radius: var(--radius-xl); display: flex; flex-direction: column; justify-content: space-between; gap: 28px; }
    .profile-top { display: flex; align-items: center; gap: 15px; }
    .profile-logo { width: 68px; height: 68px; object-fit: cover; border-radius: 21px; border: 1px solid var(--line-strong); background: linear-gradient(135deg, var(--purple), var(--pink)); }
    .profile-title { margin: 0; font-size: 22px; }
    .profile-sub { margin: 4px 0 0; color: var(--muted); }
    .quote { margin: 0; padding: 18px; color: #d8daea; line-height: 1.75; border: 1px solid var(--line); border-radius: 16px; background: rgba(255,255,255,.035); }
    .status-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
    .stat { padding: 14px; border: 1px solid var(--line); border-radius: 14px; background: rgba(255,255,255,.035); }
    .stat-label { display: block; color: var(--subtle); font-size: 12px; }
    .stat-value { display: block; margin-top: 5px; color: white; font-size: 17px; font-weight: 780; }
    .online { display: inline-flex; align-items: center; gap: 7px; color: var(--cyan); }
    .online::before { content: ""; width: 8px; height: 8px; border-radius: 50%; background: var(--cyan); box-shadow: 0 0 12px currentColor; }

    .section { margin-top: 72px; }
    .section-head { margin-bottom: 22px; display: flex; justify-content: space-between; gap: 18px; align-items: end; }
    .section-kicker { margin: 0 0 8px; color: var(--cyan); font-size: 13px; font-weight: 760; letter-spacing: .12em; text-transform: uppercase; }
    h2 { margin: 0; font-size: clamp(31px, 5vw, 50px); letter-spacing: -.045em; }
    .section-desc { margin: 0; max-width: 440px; color: var(--muted); line-height: 1.65; }
    .resource-grid { display: grid; grid-template-columns: repeat(12, 1fr); gap: 16px; }
    .resource {
      position: relative;
      min-height: 200px;
      padding: 24px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      overflow: hidden;
      border: 1px solid var(--line);
      border-radius: var(--radius-lg);
      color: inherit;
      background: rgba(20, 23, 41, .83);
      text-decoration: none;
      transition: border-color .15s ease, background-color .15s ease;
    }
    .resource:hover { transform: translateY(-4px); border-color: rgba(139,124,255,.48); background: rgba(27, 29, 51, .95); }
    .resource.large { grid-column: span 6; }
    .resource.small { grid-column: span 3; }
    .resource-icon { width: 46px; height: 46px; display: grid; place-items: center; border: 1px solid var(--line); border-radius: 14px; background: rgba(255,255,255,.06); font-size: 23px; }
    .resource h3 { margin: 26px 0 8px; font-size: 21px; }
    .resource p { margin: 0; color: var(--muted); line-height: 1.55; }
    .resource-meta { margin-top: 24px; display: flex; align-items: center; justify-content: space-between; color: var(--subtle); font-size: 13px; }
    .resource-arrow { color: var(--cyan); font-size: 21px; }
    .tag { width: fit-content; padding: 6px 9px; border: 1px solid var(--line); border-radius: 999px; color: var(--muted); background: rgba(255,255,255,.04); font-size: 12px; }
    .section-card { padding: 27px; border-radius: var(--radius-lg); }
    .privacy-note { padding: 13px 15px; border: 1px solid rgba(255,209,102,.24); border-radius: 13px; color: #ddd3ae; background: rgba(255,209,102,.06); font-size: 13px; line-height: 1.55; }

    .community-grid { display: grid; grid-template-columns: 1.2fr .8fr; gap: 16px; }
    .contact-list { display: grid; gap: 10px; margin-top: 22px; }
    .contact-row { padding: 15px 16px; display: flex; align-items: center; justify-content: space-between; gap: 12px; border: 1px solid var(--line); border-radius: 14px; background: rgba(255,255,255,.035); text-decoration: none; }
    .contact-row:hover { border-color: rgba(86,230,193,.34); }
    .contact-label { display: flex; align-items: center; gap: 11px; }
    .contact-copy { color: var(--subtle); font-size: 13px; }
    .community-cta { background: linear-gradient(145deg, rgba(255,122,184,.12), rgba(139,124,255,.09)), var(--surface); }
    .community-cta h3 { margin: 0; font-size: 27px; }
    .community-cta p { color: var(--muted); line-height: 1.7; }

    footer { max-width: var(--max); margin: 0 auto; padding: 0 18px 38px; color: var(--subtle); }
    .footer-inner { padding-top: 24px; display: flex; align-items: center; justify-content: space-between; gap: 16px; border-top: 1px solid var(--line); }
    .footer-links { display: flex; flex-wrap: wrap; gap: 16px; }
    .footer-links a { text-decoration: none; }
    .footer-links a:hover { color: white; }

    .toast { position: fixed; left: 50%; bottom: 24px; z-index: 50; width: min(440px, calc(100vw - 32px)); padding: 14px 16px; border: 1px solid var(--line-strong); border-radius: 14px; color: white; background: rgba(16,18,31,.94); box-shadow: var(--shadow); transform: translate(-50%, 120px); opacity: 0; transition: transform .25s ease, opacity .25s ease; pointer-events: none; }
    .toast.show { transform: translate(-50%, 0); opacity: 1; }

    dialog { width: min(460px, calc(100vw - 32px)); padding: 0; border: 1px solid var(--line-strong); border-radius: 22px; color: var(--text); background: #141727; box-shadow: var(--shadow); }
    dialog::backdrop { background: rgba(2,3,8,.78); }
    .dialog-body { padding: 26px; }
    .dialog-top { display: flex; align-items: start; justify-content: space-between; gap: 20px; }
    .dialog-top h3 { margin: 0; font-size: 24px; }
    .qr { width: 220px; margin: 24px auto 16px; border-radius: 18px; border: 8px solid white; background: white; }
    .dialog-caption { color: var(--muted); text-align: center; }

    @media (max-width: 900px) {
      .hero { grid-template-columns: 1fr; }
      .hero-main { min-height: 470px; }
      .resource.large { grid-column: span 6; }
      .resource.small { grid-column: span 6; }
    }

    @media (max-width: 680px) {
      .nav-links { display: none; position: absolute; left: 18px; right: 18px; top: 86px; padding: 10px; flex-direction: column; align-items: stretch; border: 1px solid var(--line); border-radius: 16px; background: rgba(10,12,24,.96); box-shadow: var(--shadow); }
      .nav-links.open { display: flex; }
      .nav-links a { justify-content: flex-start; }
      .menu-btn { display: inline-flex; }
      main { padding-top: 28px; }
      .hero-main { padding: 34px 25px; min-height: 440px; }
      .section { margin-top: 56px; }
      .section-head { display: block; }
      .section-desc { margin-top: 12px; }
      .resource.large, .resource.small { grid-column: 1 / -1; }
      .resource { min-height: 185px; }
      .footer-inner { align-items: flex-start; flex-direction: column; }
      .btn { width: 100%; }
      .profile-card { padding: 22px; }
    }

    .scroll-progress { position: fixed; inset: 0 0 auto; z-index: 60; height: 2px; transform-origin: left; transform: scaleX(0); background: linear-gradient(90deg, var(--purple), var(--cyan)); pointer-events: none; }
    .nav-links a.active { color: white; background: rgba(139,124,255,.13); border-color: rgba(139,124,255,.28); }
    .hero-main::before { content: ""; position: absolute; width: 240px; height: 240px; right: 8%; top: -100px; border-radius: 50%; background: rgba(139,124,255,.13); pointer-events: none; }
    .gradient-text { background-size: 220% auto; }
    .profile-avatar-wrap { position: relative; width: 82px; height: 82px; display: grid; place-items: center; flex: 0 0 auto; }
    .profile-avatar-wrap::before { content: ""; position: absolute; inset: 0; border-radius: 25px; background: linear-gradient(135deg, var(--purple), var(--cyan)); opacity: .35; }
    .profile-avatar-wrap::after { content: ""; position: absolute; inset: 3px; border-radius: 22px; background: #15182a; }
    .profile-avatar-wrap .profile-logo { position: relative; z-index: 1; width: 68px; height: 68px; }
    .online::before { animation: statusPulse 2.4s ease-in-out infinite; }
    .resource-icon svg, .contact-icon svg, .mobile-icon svg, .icon-btn svg { width: 22px; height: 22px; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
    .resource-icon { color: #d8d2ff; transition: transform .22s ease, color .22s ease, border-color .22s ease; }
    .resource:hover .resource-icon { transform: translateY(-3px) rotate(-2deg); color: var(--cyan); border-color: rgba(86,230,193,.32); }
    .resource-arrow { display: inline-block; transition: transform .22s ease; }
    .resource:hover .resource-arrow { transform: translate(3px,-3px); }
    .contact-icon { width: 30px; height: 30px; display: grid; place-items: center; color: var(--cyan); }
    .contact-row { transition: border-color .15s ease, background-color .15s ease; }
    .contact-row:hover { transform: translateX(3px); background: rgba(255,255,255,.055); }
    .dialog-actions .btn { min-height: 44px; padding: 0 12px; }
    .mobile-dock { display: none; }
    
    /* reveal visible */ { opacity: 1; transform: none; }
    @keyframes titleFlow { to { background-position: 220% center; } }
    @keyframes ringSpin { to { transform: rotate(360deg); } }
    @keyframes auraFloat { 50% { transform: translate(-26px, 20px) scale(1.08); } }
    @keyframes statusPulse { 50% { opacity: .55; box-shadow: 0 0 20px currentColor; } }

    /* V4: 卡片悬停彩色光晕 */
    .resource:hover { border-color: var(--line-strong); }
    .resource:hover { border-color: var(--line-strong); }
    .resource:hover { border-color: var(--line-strong); }
    .resource:hover { border-color: var(--line-strong); }
    .resource:hover { border-color: var(--line-strong); }
    .resource:hover { border-color: var(--line-strong); }

    /* V4: 资源卡片交错入场 */
    .resource
    .resource/* reveal visible */ { opacity: 1; transform: none; transition: opacity .5s ease, transform .5s ease; }
    .resource/* reveal visible */:nth-child(1) { transition-delay: 0s; }
    .resource/* reveal visible */:nth-child(2) { transition-delay: .08s; }
    .resource/* reveal visible */:nth-child(3) { transition-delay: .16s; }
    .resource/* reveal visible */:nth-child(4) { transition-delay: .24s; }
    .resource/* reveal visible */:nth-child(5) { transition-delay: .32s; }
    .resource/* reveal visible */:nth-child(6) { transition-delay: .4s; }

    /* V4: 自定义选中色 */
    ::selection { background: rgba(139,124,255,.32); color: #fff; }
    ::-moz-selection { background: rgba(139,124,255,.32); color: #fff; }

    /* V4: 滚动条美化 */
    ::-webkit-scrollbar { width: 10px; }
    ::-webkit-scrollbar-track { background: rgba(255,255,255,.03); }
    ::-webkit-scrollbar-thumb { border-radius: 8px; background: rgba(139,124,255,.3); border: 2px solid transparent; background-clip: content-box; }
    ::-webkit-scrollbar-thumb:hover { background: rgba(139,124,255,.5); background-clip: content-box; }
    ::-webkit-scrollbar-corner { background: transparent; }

    /* V4: 返回顶部按钮 */
    .back-top { position: fixed; right: 24px; bottom: 24px; z-index: 35; width: 48px; height: 48px; display: grid; place-items: center; border: 1px solid var(--line-strong); border-radius: 14px; background: rgba(10,12,24,.88); box-shadow: 0 10px 30px rgba(0,0,0,.3); color: var(--cyan); cursor: pointer; opacity: 0; transform: translateY(20px) scale(.85); pointer-events: none; transition: opacity .3s ease, transform .3s ease; }
    .back-top.visible { opacity: 1; transform: none; pointer-events: auto; }
    .back-top:hover { background: rgba(139,124,255,.15); border-color: rgba(139,124,255,.35); }
    .back-top svg { width: 22px; height: 22px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }
    @media (max-width: 680px) { .back-top { right: 12px; bottom: 96px; } }

    /* V4: 搜索筛选栏 */
    .resource-toolbar { margin-bottom: 20px; display: flex; gap: 10px; flex-wrap: wrap; align-items: center; }
    .search-box { flex: 1; min-width: 200px; position: relative; }
    .search-box input { width: 100%; min-height: 46px; padding: 0 16px 0 42px; border: 1px solid var(--line); border-radius: 14px; color: var(--text); background: rgba(20,23,41,.8); font-size: 15px; outline: none; transition: border-color .2s ease; }
    .search-box input:focus { border-color: var(--purple); }
    .search-box input::placeholder { color: var(--subtle); }
    .search-box svg { position: absolute; left: 14px; top: 50%; transform: translateY(-50%); width: 18px; height: 18px; fill: none; stroke: var(--subtle); stroke-width: 2; pointer-events: none; }
    .filter-chips { display: flex; gap: 8px; flex-wrap: wrap; }
    .chip { padding: 8px 14px; border: 1px solid var(--line); border-radius: 999px; color: var(--muted); background: rgba(255,255,255,.04); font-size: 13px; cursor: pointer; transition: color .2s ease, background-color .2s ease, border-color .2s ease; }
    .chip:hover { color: white; background: rgba(255,255,255,.07); }
    .chip.active { color: #0a0b12; background: linear-gradient(135deg, var(--purple), var(--cyan)); border-color: transparent; font-weight: 700; }
    .resource.hidden-by-filter { display: none; }

    /* V4: 资源卡片收藏星标 */
    .fav-btn { position: absolute; top: 14px; right: 14px; width: 32px; height: 32px; display: grid; place-items: center; border: 1px solid var(--line); border-radius: 10px; background: rgba(255,255,255,.05); color: var(--subtle); cursor: pointer; z-index: 2; transition: color .2s ease, background-color .2s ease; }
    .fav-btn:hover { color: var(--amber); background: rgba(255,209,102,.08); }
    .fav-btn.fav-active { color: var(--amber); background: rgba(255,209,102,.1); border-color: rgba(255,209,102,.3); }
    .fav-btn svg { width: 16px; height: 16px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }
    .fav-btn.fav-active svg { fill: currentColor; }
    .resource.fav-saved { order: -1; }

    /* V4: Web Share 按钮 */
    .share-btn { position: absolute; bottom: 14px; right: 14px; width: 30px; height: 30px; display: grid; place-items: center; border: none; border-radius: 8px; background: transparent; color: var(--subtle); cursor: pointer; opacity: 0; transition: opacity .2s ease, color .2s ease; z-index: 2; }
    .resource:hover .share-btn { opacity: .7; }
    .share-btn:hover { opacity: 1 !important; color: var(--cyan); }
    .share-btn svg { width: 16px; height: 16px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

    /* V4: 主题切换变量 */
    body.light-theme {
      --bg: #f5f6fb;
      --surface: rgba(255,255,255,.88);
      --surface-strong: #ffffff;
      --surface-soft: rgba(0,0,0,.04);
      --line: rgba(0,0,0,.1);
      --line-strong: rgba(0,0,0,.16);
      --text: #1a1d2e;
      --muted: #555a72;
      --subtle: #8b90a6;
      color-scheme: light;
    }
    body.light-theme .navbar { background: rgba(255,255,255,.82); }
    body.light-theme .mobile-dock { background: rgba(255,255,255,.94); }
    body.light-theme .resource { background: rgba(255,255,255,.85); }
    body.light-theme dialog { background: #f8f9fc; }
    body.light-theme .search-box input { background: rgba(255,255,255,.8); }
    body.light-theme .back-top { background: rgba(255,255,255,.88); }
    body.light-theme .toast { background: rgba(255,255,255,.94); }

    /* V4: 主题色选择器 */
    .theme-picker { display: flex; gap: 6px; align-items: center; }
    .swatch { width: 22px; height: 22px; border-radius: 50%; border: 2px solid var(--line-strong); cursor: pointer; transition: border-color .15s ease, background-color .15s ease; }
    .swatch:hover { transform: scale(1.15); }
    .swatch.active { border-color: var(--text); transform: scale(1.1); }

    /* V4: 字号调节 */
    .font-ctrl { display: flex; gap: 4px; align-items: center; }
    .font-btn { width: 32px; height: 32px; display: grid; place-items: center; border: 1px solid var(--line); border-radius: 9px; background: transparent; color: var(--muted); cursor: pointer; font-size: 14px; font-weight: 700; transition: color .2s ease, background-color .2s ease; }
    .font-btn:hover { color: white; background: var(--surface-soft); }
    body.font-sm { font-size: 14px; }
    body.font-md { font-size: 16px; }
    body.font-lg { font-size: 18px; }

    /* V4: 高对比模式 */
    body.high-contrast {
      --text: #ffffff;
      --muted: #d0d4e8;
      --subtle: #b0b4c8;
      --line: rgba(255,255,255,.3);
      --line-strong: rgba(255,255,255,.5);
      --bg: #000000;
      --surface: rgba(0,0,0,.9);
    }
    body.high-contrast .resource { border-color: rgba(255,255,255,.35); }
    body.high-contrast a:focus-visible, body.high-contrast button:focus-visible { outline: 4px solid #ffff00; }

    /* V4: Ctrl+K 面板 */
    .cmd-overlay { position: fixed; inset: 0; z-index: 80; display: none; align-items: flex-start; justify-content: center; padding-top: 15vh; background: rgba(2,3,8,.72); }
    .cmd-overlay.open { display: flex; }
    .cmd-box { width: min(560px, calc(100vw - 32px)); border: 1px solid var(--line-strong); border-radius: 18px; background: #141727; box-shadow: var(--shadow); overflow: hidden; }
    .cmd-input-wrap { display: flex; align-items: center; gap: 10px; padding: 16px 18px; border-bottom: 1px solid var(--line); }
    .cmd-input-wrap svg { width: 20px; height: 20px; fill: none; stroke: var(--subtle); stroke-width: 2; flex: 0 0 auto; }
    .cmd-input-wrap input { flex: 1; border: none; background: transparent; color: var(--text); font-size: 17px; outline: none; }
    .cmd-input-wrap input::placeholder { color: var(--subtle); }
    .cmd-results { max-height: 340px; overflow-y: auto; padding: 8px; }
    .cmd-item { display: flex; align-items: center; gap: 12px; padding: 12px 14px; border-radius: 12px; color: var(--muted); cursor: pointer; text-decoration: none; transition: background-color .15s ease; }
    .cmd-item:hover, .cmd-item.selected { background: rgba(139,124,255,.12); color: white; }
    .cmd-item svg { width: 20px; height: 20px; fill: none; stroke: currentColor; stroke-width: 1.8; flex: 0 0 auto; }
    .cmd-item small { margin-left: auto; color: var(--subtle); font-size: 12px; }
    .cmd-empty { padding: 24px; text-align: center; color: var(--subtle); }
    .cmd-hint { display: flex; gap: 8px; padding: 10px 14px; border-top: 1px solid var(--line); font-size: 12px; color: var(--subtle); }
    .kbd { padding: 2px 7px; border: 1px solid var(--line); border-radius: 6px; font-family: monospace; font-size: 11px; }

    /* V4: FAQ */
    .faq-item { margin-top: 10px; border: 1px solid var(--line); border-radius: 14px; background: rgba(255,255,255,.03); overflow: hidden; }
    .faq-item summary { padding: 16px; cursor: pointer; font-weight: 600; color: var(--text); display: flex; align-items: center; gap: 8px; list-style: none; }
    .faq-item summary::-webkit-details-marker { display: none; }
    .faq-item summary::after { content: "▾"; margin-left: auto; color: var(--subtle); transition: transform .2s ease; }
    .faq-item[open] summary::after { transform: rotate(180deg); }
    .faq-item p { margin: 0; padding: 0 16px 16px; color: var(--muted); line-height: 1.7; }

    /* V4: 站点统计 */
    .stats-bar { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-bottom: 20px; }
    .stat-card { padding: 16px; border: 1px solid var(--line); border-radius: 14px; background: rgba(255,255,255,.03); text-align: center; }
    .stat-card .num { font-size: 28px; font-weight: 850; color: var(--text); letter-spacing: -.03em; }
    .stat-card .label { display: block; margin-top: 4px; color: var(--subtle); font-size: 12px; }
    @media (max-width: 680px) { .stats-bar { grid-template-columns: repeat(2, 1fr); } }

    /* V4: 资源状态指示 */
    .status-dot { display: inline-flex; align-items: center; gap: 5px; font-size: 11px; }
    .status-dot::before { content: ""; width: 7px; height: 7px; border-radius: 50%; }
    .status-dot.ok::before { background: var(--cyan); box-shadow: 0 0 8px currentColor; }
    .status-dot.warn::before { background: var(--amber); }
    .status-dot.off::before { background: var(--danger); }

    @media (max-width: 680px) {
      body { padding-bottom: 82px; }
      .nav-shell { display: none; }
      main { padding-top: 18px; }
      .hero-main { min-height: 410px; }
      .profile-avatar-wrap { width: 72px; height: 72px; }
      .profile-avatar-wrap .profile-logo { width: 60px; height: 60px; }
      .mobile-dock { position: fixed; display: grid; grid-template-columns: repeat(4,1fr); left: 12px; right: 12px; bottom: max(12px, env(safe-area-inset-bottom)); z-index: 40; padding: 8px; border: 1px solid var(--line); border-radius: 18px; background: rgba(10,12,24,.94); box-shadow: 0 16px 42px rgba(0,0,0,.42); }
      .mobile-dock a { min-height: 48px; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 3px; border-radius: 12px; color: var(--subtle); text-decoration: none; font-size: 11px; }
      .mobile-dock a.active { color: white; background: rgba(139,124,255,.15); }
      .mobile-icon { height: 20px; }
      .dialog-actions { grid-template-columns: 1fr; }
      dialog { max-height: calc(100dvh - 24px); overflow-y: auto; }
    }

    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after { scroll-behavior: auto !important; animation-duration: .01ms !important; animation-iteration-count: 1 !important; transition-duration: .01ms !important; }
      .ambient-video { display: none !important; }
      .reveal, .resource
      body { animation: none !important; }
    }
  </style>
</head>
<body>
  <div class="scroll-progress" id="scrollProgress" aria-hidden="true"></div>
  <a class="skip-link" href="#main">跳到主要内容</a>
  <video class="ambient-video" id="ambientVideo" muted loop playsinline preload="none" aria-hidden="true">
    <source src="https://yesir123886.github.io/kiro-auth-system/assets/bg-08.mp4" type="video/mp4">
  </video>

  <header class="nav-shell">
    <nav class="navbar" aria-label="主导航">
      <a class="brand" href="#top" aria-label="ANGEL 首页">
        <img class="brand-mark" src="assets/logo.png" alt="" aria-hidden="true">
        <span>ANGEL</span>
      </a>
      <div class="nav-links" id="navLinks">
        <a href="#resources">工具与服务</a>
        <a href="#community">社群联系</a>
        <a href="#faq">常见问题</a>
      </div>
      <div class="nav-actions">
        <div class="theme-picker" id="themePicker" title="切换主题色">
          <span class="swatch active" data-theme="purple" style="background:linear-gradient(135deg,#8b7cff,#56e6c1)" aria-label="紫青配色"></span>
          <span class="swatch" data-theme="ocean" style="background:linear-gradient(135deg,#3b82f6,#06b6d4)" aria-label="海洋蓝配色"></span>
          <span class="swatch" data-theme="sunset" style="background:linear-gradient(135deg,#f97316,#ec4899)" aria-label="日落橙红配色"></span>
        </div>
        <div class="font-ctrl" id="fontCtrl">
          <button class="font-btn" data-size="sm" type="button" aria-label="缩小字号">A−</button>
          <button class="font-btn" data-size="md" type="button" aria-label="默认字号" style="font-size:16px">A</button>
          <button class="font-btn" data-size="lg" type="button" aria-label="放大字号" style="font-size:18px">A+</button>
        </div>
        <button class="icon-btn" id="contrastToggle" type="button" aria-pressed="false" title="高对比模式" aria-label="切换高对比模式"><svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9"></circle><path d="M12 3v18a9 9 0 0 0 0-18Z" fill="currentColor" stroke="none"></path></svg></button>
        <button class="icon-btn" id="themeToggle" type="button" aria-pressed="false" title="浅色/深色" aria-label="切换浅色主题"><svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="4.5"></circle><path d="M12 2v3M12 19v3M2 12h3M19 12h3M5 5l2 2M17 17l2 2M5 19l2-2M17 7l2-2"></path></svg></button>
        <button class="icon-btn" id="videoToggle" type="button" aria-pressed="false" title="切换动态背景" aria-label="开启动态背景"><svg viewBox="0 0 24 24" aria-hidden="true"><rect x="3" y="5" width="18" height="14" rx="3"></rect><path d="m9 9 6 3-6 3z"></path></svg></button>
        <button class="icon-btn" id="cmdTrigger" type="button" title="快捷搜索 (Ctrl+K)" aria-label="打开快捷搜索"><svg viewBox="0 0 24 24" aria-hidden="true"><rect x="3" y="3" width="18" height="18" rx="3"></rect><path d="M9 9h6v6H9z"></path></svg></button>
        <button class="icon-btn menu-btn" id="menuToggle" type="button" aria-expanded="false" aria-controls="navLinks" aria-label="打开菜单">☰</button>
      </div>
    </nav>
  </header>

  <main id="main">
    <section class="hero" id="top" aria-labelledby="hero-title">
      <div class="hero-main">
        <div class="eyebrow">Resource portal · 2026</div>
        <h1 id="hero-title"><span class="gradient-text">ANGEL</span><br>资源导航中心</h1>
        <p class="lead">主做Kiro；ACE，入代；提卡；买参 请去下方卡网；批卡优惠力度大dd</p>
        <div class="hero-actions">
          <a class="btn btn-primary" href="#resources">浏览资源 <span aria-hidden="true">↓</span></a>
          <a class="btn" href="https://t.me/CzrANGEL0" target="_blank" rel="noopener noreferrer">前往 TG 频道 <span aria-hidden="true">↗</span></a>
        </div>
      </div>

      <aside class="profile-card" aria-label="站点信息">
        <div class="profile-top">
          <div class="profile-avatar-wrap"><img class="profile-logo" src="assets/logo.png" alt="ANGEL 标志" loading="eager" width="68" height="68"></div>
          <div>
            <h2 class="profile-title">ANGEL</h2>
            <p class="profile-sub">P蛋爱露出</p>
          </div>
        </div>
        <blockquote class="quote">“世界以痛吻我，我却报之以歌。”</blockquote>
        <div class="status-grid">
          <div class="stat"><span class="stat-label">系统状态</span><strong class="stat-value online">在线</strong></div>
          <div class="stat"><span class="stat-label">当前时间</span><strong class="stat-value" id="clock">--:--</strong></div>
          <div class="stat"><span class="stat-label">今日问候</span><strong class="stat-value" id="greeting">晚上好</strong></div>
          <div class="stat"><span class="stat-label">页面版本</span><strong class="stat-value">v5.0</strong></div>
        </div>
      </aside>
    </section>

    <section class="section" id="resources" aria-labelledby="resources-title">
      <header class="section-head">
        <div>
          <p class="section-kicker">Resources</p>
          <h2 id="resources-title">工具与服务</h2>
        </div>
        <p class="section-desc"></p>
      </header>
      <div class="stats-bar">
        <div class="stat-card"><span class="num" id="statTotal">6</span><span class="label">资源总数</span></div>
        <div class="stat-card"><span class="num" id="statFav">0</span><span class="label">已收藏</span></div>
        <div class="stat-card"><span class="num">2</span><span class="label">社群入口</span></div>
        <div class="stat-card"><span class="num" id="statUpdate">今日</span><span class="label">最后更新</span></div>
      </div>
      <div class="resource-toolbar">
        <div class="search-box">
          <svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"></circle><path d="m21 21-4.3-4.3"></path></svg>
          <input type="text" id="resourceSearch" placeholder="搜索资源名称或描述…" autocomplete="off">
        </div>
        <div class="filter-chips" id="filterChips">
          <button class="chip active" data-filter="all">全部</button>
          <button class="chip" data-filter="tool">工具</button>
          <button class="chip" data-filter="buy">购买</button>
          <button class="chip" data-filter="community">社群</button>
          <button class="chip" data-filter="event">活动</button>
        </div>
      </div>
      <div class="resource-grid">
        <a class="resource large" data-tags="tool" data-name="辅助与工具合集" href="https://1842160496.share.123pan.cn/123pan/hAs0Td-Qisbv" target="_blank" rel="noopener noreferrer">
          <button class="fav-btn" data-fav="0" type="button" aria-label="收藏" tabindex="-1"><svg viewBox="0 0 24 24"><path d="M12 3l2.5 6h6.5l-5 4.5 1.8 6.5L12 16.5 6.2 20l1.8-6.5-5-4.5h6.5z"></path></svg></button>
          <div><span class="resource-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 8h16v11H4z"></path><path d="M9 8V5h6v3M4 12h16M10 12v2h4v-2"></path></svg></span><h3>辅助与工具合集</h3><p>常用资源统一收纳，前往 123 云盘查看和下载。</p></div>
          <div class="resource-meta"><span class="status-dot ok">正常</span><span class="tag">资源合集</span><span class="resource-arrow" aria-hidden="true">↗</span></div>
        </a>
        <a class="resource large" data-tags="event" data-name="Kiro 专属抽奖" href="https://cj.hysafe.top/" target="_blank" rel="noopener noreferrer">
          <button class="fav-btn" data-fav="0" type="button" aria-label="收藏" tabindex="-1"><svg viewBox="0 0 24 24"><path d="M12 3l2.5 6h6.5l-5 4.5 1.8 6.5L12 16.5 6.2 20l1.8-6.5-5-4.5h6.5z"></path></svg></button>
          <div><span class="resource-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 10h16v10H4zM3 7h18v3H3zM12 7v13"></path><path d="M12 7c-2.7 0-5-1-5-3 0-1.1.9-2 2-2 1.7 0 3 2.3 3 5Zm0 0c2.7 0 5-1 5-3 0-1.1-.9-2-2-2-1.7 0-3 2.3-3 5Z"></path></svg></span><h3>Kiro 专属抽奖</h3><p>前往活动页面参与抽奖，外部页面将在新标签页打开。</p></div>
          <div class="resource-meta"><span class="status-dot ok">正常</span><span class="tag">活动入口</span><span class="resource-arrow" aria-hidden="true">↗</span></div>
        </a>
        <a class="resource small" data-tags="buy" data-name="主卡网" href="https://buy.jry0.com/shop/E0ZBFJ5E" target="_blank" rel="noopener noreferrer">
          <button class="fav-btn" data-fav="0" type="button" aria-label="收藏" tabindex="-1"><svg viewBox="0 0 24 24"><path d="M12 3l2.5 6h6.5l-5 4.5 1.8 6.5L12 16.5 6.2 20l1.8-6.5-5-4.5h6.5z"></path></svg></button>
          <div><span class="resource-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 7h16l-1 13H5L4 7Z"></path><path d="M9 9V5a3 3 0 0 1 6 0v4"></path></svg></span><h3>主卡网</h3><p>主要购买入口。</p></div>
          <div class="resource-meta"><span class="status-dot ok">正常</span><span class="tag">购买入口</span><span class="resource-arrow" aria-hidden="true">↗</span></div>
        </a>
        <a class="resource small" data-tags="buy" data-name="备用卡网" href="http://ptopd.top/" target="_blank" rel="noopener noreferrer">
          <button class="fav-btn" data-fav="0" type="button" aria-label="收藏" tabindex="-1"><svg viewBox="0 0 24 24"><path d="M12 3l2.5 6h6.5l-5 4.5 1.8 6.5L12 16.5 6.2 20l1.8-6.5-5-4.5h6.5z"></path></svg></button>
          <div><span class="resource-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M12 3 20 6v5c0 5-3.4 8.6-8 10-4.6-1.4-8-5-8-10V6l8-3Z"></path><path d="m9 12 2 2 4-4"></path></svg></span><h3>备用卡网</h3><p>主卡网不可用时使用备用通道。</p></div>
          <div class="resource-meta"><span class="status-dot warn">HTTP</span><span class="tag">备用入口</span><span class="resource-arrow" aria-hidden="true">↗</span></div>
        </a>
        <a class="resource small" data-tags="community" data-name="QQ 闲聊群" href="https://qm.qq.com/q/K4BdkG1FCi" target="_blank" rel="noopener noreferrer">
          <button class="fav-btn" data-fav="0" type="button" aria-label="收藏" tabindex="-1"><svg viewBox="0 0 24 24"><path d="M12 3l2.5 6h6.5l-5 4.5 1.8 6.5L12 16.5 6.2 20l1.8-6.5-5-4.5h6.5z"></path></svg></button>
          <div><span class="resource-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 5h16v12H9l-5 4V5Z"></path><path d="M8 9h8M8 13h5"></path></svg></span><h3>QQ 闲聊群</h3><p>加入日常交流社群。</p></div>
          <div class="resource-meta"><span class="status-dot ok">正常</span><span class="tag">社群</span><span class="resource-arrow" aria-hidden="true">↗</span></div>
        </a>
        <a class="resource small" data-tags="community" data-name="Telegram 频道" href="https://t.me/CzrANGEL0" target="_blank" rel="noopener noreferrer">
          <button class="fav-btn" data-fav="0" type="button" aria-label="收藏" tabindex="-1"><svg viewBox="0 0 24 24"><path d="M12 3l2.5 6h6.5l-5 4.5 1.8 6.5L12 16.5 6.2 20l1.8-6.5-5-4.5h6.5z"></path></svg></button>
          <div><span class="resource-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M22 4 2 11l6 2 2 6 3-4 5 4 4-15Z"></path><path d="m8 13 8-5-5 7"></path></svg></span><h3>Telegram 频道</h3><p>获取频道最新消息。</p></div>
          <div class="resource-meta"><span class="status-dot ok">正常</span><span class="tag">@CzrANGEL0</span><span class="resource-arrow" aria-hidden="true">↗</span></div>
        </a>
      </div>
    </section>

    <section class="section" id="community" aria-labelledby="community-title">
      <header class="section-head">
        <div><p class="section-kicker">Community</p><h2 id="community-title">社群与联系</h2></div>
        <p class="section-desc"></p>
      </header>
      <div class="community-grid">
        <div class="section-card">
          <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:4px">
            <h3>快速联系</h3>
            <button class="btn" id="copyAllContacts" type="button" style="min-height:38px;padding:0 12px;font-size:13px">复制全部</button>
          </div>
          <div class="contact-list">
            <a class="contact-row" href="https://t.me/CzrANGEL0" target="_blank" rel="noopener noreferrer"><span class="contact-label"><span class="contact-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M22 4 2 11l6 2 2 6 3-4 5 4 4-15Z"></path><path d="m8 13 8-5-5 7"></path></svg></span>Telegram 频道</span><span class="contact-copy">@CzrANGEL0 ↗</span></a>
            <a class="contact-row" href="https://qm.qq.com/q/K4BdkG1FCi" target="_blank" rel="noopener noreferrer"><span class="contact-label"><span class="contact-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M4 5h16v12H9l-5 4V5Z"></path><path d="M8 9h8M8 13h5"></path></svg></span>QQ 闲聊群</span><span class="contact-copy">立即加入 ↗</span></a>
            <button class="contact-row" id="copyQQ" type="button" style="width:100%; color:inherit; cursor:pointer"><span class="contact-label"><span class="contact-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><rect x="5" y="5" width="14" height="14" rx="2"></rect><path d="M9 5V3h6v2M9 9h6M9 13h4"></path></svg></span>复制 QQ 群号</span><span class="contact-copy">753430447</span></button>
            <a class="contact-row" href="https://qm.qq.com/q/qHlgSLhEVG" target="_blank" rel="noopener noreferrer"><span class="contact-label"><span class="contact-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><circle cx="9" cy="8" r="3.5"></circle><path d="M3 20c0-3.3 2.7-6 6-6s6 2.7 6 6"></path><circle cx="17" cy="10" r="3"></circle><path d="M14 20c0-2.5 1.5-4.5 3.5-5.5"></path></svg></span>备用群聊</span><span class="contact-copy">打开链接 ↗</span></a>
          </div>
        </div>
        <div class="section-card community-cta">
          <h3>咨询代理批卡</h3>
          <p>通过 Telegram 联系 @ANGELDonk。你也可以先查看二维码，再决定是否添加。</p>
          <button class="btn" id="qrButton" type="button">查看联系二维码</button>
        </div>
      </div>
    </section>

    <section class="section" id="faq" aria-labelledby="faq-title">
      <header class="section-head">
        <div><p class="section-kicker">FAQ</p><h2 id="faq-title">常见问题</h2></div>
        <p class="section-desc">点击展开查看解答。</p>
      </header>
      <div>
        <details class="faq-item"><summary>怎么购买卡密？</summary><p>点击"主卡网"进入购买页面，按页面指引完成支付即可。如果主卡网无法访问，可使用"备用卡网"。</p></details>
        <details class="faq-item"><summary>备用卡网安全吗？</summary><p>备用卡网目前使用 HTTP 协议（非 HTTPS），建议仅在主卡网不可用时使用，并注意不要在页面输入敏感信息。</p></details>
        <details class="faq-item"><summary>怎么联系代理批卡？</summary><p>通过 Telegram 联系 @ANGELDonk，可点击"查看联系二维码"扫码添加。请核对账号名称，谨防假冒。</p></details>
        <details class="faq-item"><summary>QQ 群号是多少？</summary><p>QQ 闲聊群号：753430447，可点击"复制 QQ 群号"直接复制，或点击"QQ 闲聊群"直接跳转加入。</p></details>
        <details class="faq-item"><summary>天气定位会泄露隐私吗？</summary><p>不会。定位仅在点击"获取我的天气"后请求，且仅用于向天气服务查询当前区域天气。拒绝定位不影响其他功能。</p></details>
      </div>
    </section>

  </main>

  <footer>
    <div class="footer-inner">
      <span>© 2026 ANGEL · 资源导航</span>
      <div class="footer-links"><a href="#top">返回顶部</a><a href="#community">社群联系</a><span>v5.0</span></div>
    </div>
  </footer>

  <nav class="mobile-dock" aria-label="移动端快速导航">
    <a href="#top" class="active"><span class="mobile-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M3 11l9-8 9 8M5 10v10h14V10"></path></svg></span>首页</a>
    <a href="#resources"><span class="mobile-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="7" rx="1.5"></rect><rect x="14" y="3" width="7" height="7" rx="1.5"></rect><rect x="3" y="14" width="7" height="7" rx="1.5"></rect><rect x="14" y="14" width="7" height="7" rx="1.5"></rect></svg></span>资源</a>
    <a href="#community"><span class="mobile-icon" aria-hidden="true"><svg viewBox="0 0 24 24"><circle cx="9" cy="8" r="3.5"></circle><path d="M3 20c0-3.3 2.7-6 6-6s6 2.7 6 6"></path><circle cx="17" cy="10" r="3"></circle><path d="M14 20c0-2.5 1.5-4.5 3.5-5.5"></path></svg></span>社群</a>
  </nav>

  <dialog id="qrDialog" aria-labelledby="qrTitle">
    <div class="dialog-body">
      <div class="dialog-top"><div><h3 id="qrTitle">Telegram 联系二维码</h3><p style="color:var(--muted); margin-bottom:0">账号：@ANGELDonk</p></div><button class="icon-btn" id="closeDialog" type="button" aria-label="关闭二维码弹窗">×</button></div>
      <img class="qr" src="assets/telegram-angel-donk-qr.png" alt="Telegram 账号 ANGELDonk 的二维码" loading="lazy" width="220" height="220">
      <p class="dialog-caption">扫码后请核对账号名称，谨防假冒。</p>
      <div class="dialog-actions">
        <button class="btn" id="copyTelegram" type="button">复制账号 @ANGELDonk</button>
        <a class="btn btn-primary" href="https://t.me/ANGELDonk" target="_blank" rel="noopener noreferrer">打开 Telegram</a>
      </div>
    </div>
  </dialog>

  <div class="cmd-overlay" id="cmdOverlay" aria-hidden="true">
    <div class="cmd-box" role="dialog" aria-label="快捷搜索">
      <div class="cmd-input-wrap">
        <svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"></circle><path d="m21 21-4.3-4.3"></path></svg>
        <input type="text" id="cmdInput" placeholder="搜索资源或跳转到版块…" autocomplete="off">
      </div>
      <div class="cmd-results" id="cmdResults"></div>
      <div class="cmd-hint"><span class="kbd">↑↓</span> 选择</div>
    </div>
  </div>

  <button class="back-top" id="backTop" type="button" aria-label="返回顶部">
    <svg viewBox="0 0 24 24"><path d="M12 19V5M5 12l7-7 7 7"></path></svg>
  </button>

  <div class="toast" id="toast" role="status" aria-live="polite"></div>

  <script>
    const $ = (selector) => document.querySelector(selector);
    const toast = $("#toast");
    let toastTimer;

    function showToast(message) {
      clearTimeout(toastTimer);
      toast.textContent = message;
      toast.classList.add("show");
      toastTimer = setTimeout(() => toast.classList.remove("show"), 2600);
    }

    function updateClock() {
      const now = new Date();
      $("#clock").textContent = now.toLocaleTimeString("zh-CN", { hour: "2-digit", minute: "2-digit" });
      const hour = now.getHours();
      $("#greeting").textContent = hour < 6 ? "夜深了" : hour < 11 ? "早上好" : hour < 14 ? "中午好" : hour < 18 ? "下午好" : "晚上好";
    }
    updateClock();
    setInterval(updateClock, 30000);

    const menuToggle = $("#menuToggle");
    const navLinks = $("#navLinks");
    menuToggle.addEventListener("click", () => {
      const open = navLinks.classList.toggle("open");
      menuToggle.setAttribute("aria-expanded", String(open));
      menuToggle.setAttribute("aria-label", open ? "关闭菜单" : "打开菜单");
    });
    navLinks.addEventListener("click", () => {
      navLinks.classList.remove("open");
      menuToggle.setAttribute("aria-expanded", "false");
    });

    const videoToggle = $("#videoToggle");
    const ambientVideo = $("#ambientVideo");
    videoToggle.addEventListener("click", async () => {
      const on = document.body.classList.toggle("video-on");
      videoToggle.setAttribute("aria-pressed", String(on));
      videoToggle.setAttribute("aria-label", on ? "关闭动态背景" : "开启动态背景");
      if (on) {
        try { await ambientVideo.play(); showToast("动态背景已开启"); }
        catch { document.body.classList.remove("video-on"); showToast("浏览器阻止了视频播放"); }
      } else {
        ambientVideo.pause();
        showToast("动态背景已关闭");
      }
    });

    $("#copyQQ").addEventListener("click", async () => {
      try {
        await navigator.clipboard.writeText("753430447");
        showToast("QQ群号已复制：753430447");
      } catch {
        showToast("复制失败，请手动复制：753430447");
      }
    });

    const qrDialog = $("#qrDialog");
    $("#qrButton").addEventListener("click", () => qrDialog.showModal());
    $("#closeDialog").addEventListener("click", () => qrDialog.close());
    qrDialog.addEventListener("click", (event) => {
      const rect = qrDialog.getBoundingClientRect();
      const outside = event.clientX < rect.left || event.clientX > rect.right || event.clientY < rect.top || event.clientY > rect.bottom;
      if (outside) qrDialog.close();
    });

    /* === V3 新增交互 === */

    // 滚动进度条
    const scrollProgress = $("#scrollProgress");
    function updateScrollProgress() {
      const scrollTop = window.scrollY;
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      const ratio = docHeight > 0 ? scrollTop / docHeight : 0;
      scrollProgress.style.transform = `scaleX(${Math.min(1, ratio)})`;
    }
    window.addEventListener("scroll", updateScrollProgress, { passive: true });
    updateScrollProgress();

    $("#copyTelegram").addEventListener("click", async () => {
      try {
        await navigator.clipboard.writeText("@ANGELDonk");
        showToast("已复制：@ANGELDonk");
      } catch {
        showToast("复制失败，请手动复制：@ANGELDonk");
      }
    });

    /* === V4 新增功能 === */

    // 返回顶部按钮
    const backTop = $("#backTop");
    window.addEventListener("scroll", () => {
      backTop.classList.toggle("visible", window.scrollY > 400);
    }, { passive: true });
    backTop.addEventListener("click", () => {
      window.scrollTo({ top: 0, behavior: "smooth" });
    });

    // 资源搜索
    const searchInput = $("#resourceSearch");
    const resources = document.querySelectorAll(".resource[data-name]");
    let currentFilter = "all";

    function applyFilter() {
      const query = (searchInput.value || "").toLowerCase().trim();
      let visibleCount = 0;
      resources.forEach((card) => {
        const name = (card.dataset.name || "").toLowerCase();
        const desc = (card.querySelector("p")?.textContent || "").toLowerCase();
        const tags = card.dataset.tags || "";
        const matchSearch = !query || name.includes(query) || desc.includes(query);
        const matchFilter = currentFilter === "all" || tags.includes(currentFilter);
        const hidden = !(matchSearch && matchFilter);
        card.classList.toggle("hidden-by-filter", hidden);
        if (!hidden) visibleCount++;
      });
    }

    searchInput.addEventListener("input", applyFilter);

    // 筛选标签
    document.querySelectorAll("#filterChips .chip").forEach((chip) => {
      chip.addEventListener("click", () => {
        document.querySelectorAll("#filterChips .chip").forEach((c) => c.classList.remove("active"));
        chip.classList.add("active");
        currentFilter = chip.dataset.filter;
        applyFilter();
      });
    });

    // 资源收藏
    const FAV_KEY = "angel-favs";
    let favs = JSON.parse(localStorage.getItem(FAV_KEY) || "[]");

    function updateFavUI() {
      let count = 0;
      document.querySelectorAll(".fav-btn").forEach((btn) => {
        const card = btn.closest(".resource");
        const name = card.dataset.name;
        const isFav = favs.includes(name);
        btn.classList.toggle("fav-active", isFav);
        card.classList.toggle("fav-saved", isFav);
        if (isFav) count++;
      });
      $("#statFav").textContent = count;
    }

    document.querySelectorAll(".fav-btn").forEach((btn) => {
      btn.addEventListener("click", (e) => {
        e.preventDefault();
        e.stopPropagation();
        const name = btn.closest(".resource").dataset.name;
        const idx = favs.indexOf(name);
        if (idx > -1) { favs.splice(idx, 1); showToast("已取消收藏"); }
        else { favs.push(name); showToast("已收藏：" + name); }
        localStorage.setItem(FAV_KEY, JSON.stringify(favs));
        updateFavUI();
      });
    });
    updateFavUI();

    // 复制全部联系方式
    $("#copyAllContacts").addEventListener("click", async () => {
      const text = "ANGEL 联系方式\nTelegram 频道：@CzrANGEL0\nQQ 闲聊群：753430447\nTelegram 代理：@ANGELDonk";
      try {
        await navigator.clipboard.writeText(text);
        showToast("全部联系方式已复制");
      } catch {
        showToast("复制失败，请手动复制");
      }
    });

    // Ctrl+K 快捷面板
    const cmdOverlay = $("#cmdOverlay");
    const cmdInput = $("#cmdInput");
    const cmdResults = $("#cmdResults");
    let cmdSelected = 0;

    const cmdData = [
      { type: "section", label: "工具与服务", href: "#resources", icon: '<rect x="3" y="3" width="7" height="7" rx="1.5"/><rect x="14" y="3" width="7" height="7" rx="1.5"/><rect x="3" y="14" width="7" height="7" rx="1.5"/><rect x="14" y="14" width="7" height="7" rx="1.5"/>' },
      { type: "section", label: "社群与联系", href: "#community", icon: '<circle cx="9" cy="8" r="3.5"/><path d="M3 20c0-3.3 2.7-6 6-6s6 2.7 6 6"/>' },
      { type: "section", label: "常见问题", href: "#faq", icon: '<circle cx="12" cy="12" r="9"/><path d="M9.5 9a2.5 2.5 0 0 1 5 0c0 2.5-2.5 2-2.5 4M12 17h.01"/>' },
      { type: "link", label: "辅助与工具合集", href: "https://1842160496.share.123pan.cn/123pan/hAs0Td-Qisbv", icon: '<path d="M4 8h16v11H4z"/><path d="M9 8V5h6v3"/>' },
      { type: "link", label: "Kiro 专属抽奖", href: "https://cj.hysafe.top/", icon: '<path d="M4 10h16v10H4zM3 7h18v3H3z"/>' },
      { type: "link", label: "主卡网", href: "https://buy.jry0.com/shop/E0ZBFJ5E", icon: '<path d="M4 7h16l-1 13H5L4 7Z"/><path d="M9 9V5a3 3 0 0 1 6 0v4"/>' },
      { type: "link", label: "备用卡网", href: "http://ptopd.top/", icon: '<path d="M12 3 20 6v5c0 5-3.4 8.6-8 10-4.6-1.4-8-5-8-10V6l8-3Z"/>' },
      { type: "link", label: "QQ 闲聊群", href: "https://qm.qq.com/q/K4BdkG1FCi", icon: '<path d="M4 5h16v12H9l-5 4V5Z"/>' },
      { type: "link", label: "Telegram 频道", href: "https://t.me/CzrANGEL0", icon: '<path d="M22 4 2 11l6 2 2 6 3-4 5 4 4-15Z"/>' },
    ];

    function renderCmdResults(query) {
      const q = query.toLowerCase().trim();
      const filtered = q ? cmdData.filter((item) => item.label.toLowerCase().includes(q)) : cmdData;
      cmdSelected = 0;
      if (!filtered.length) {
        cmdResults.innerHTML = '<div class="cmd-empty">没有找到结果</div>';
        return;
      }
      cmdResults.innerHTML = filtered.map((item, i) =>
        `<a class="cmd-item${i === 0 ? " selected" : ""}" href="${item.href}" ${item.type === "link" ? 'target="_blank" rel="noopener noreferrer"' : ""} data-idx="${i}">
          <svg viewBox="0 0 24 24">${item.icon}</svg>
          <span>${item.label}</span>
          <small>${item.type === "section" ? "跳转" : "外部"}</small>
        </a>`
      ).join("");
      cmdResults.querySelectorAll(".cmd-item").forEach((el) => {
        el.addEventListener("mouseenter", () => {
          cmdResults.querySelectorAll(".cmd-item").forEach((e) => e.classList.remove("selected"));
          el.classList.add("selected");
          cmdSelected = parseInt(el.dataset.idx);
        });
      });
    }

    function openCmd() {
      cmdOverlay.classList.add("open");
      cmdOverlay.setAttribute("aria-hidden", "false");
      cmdInput.value = "";
      renderCmdResults("");
      setTimeout(() => cmdInput.focus(), 50);
    }

    function closeCmd() {
      cmdOverlay.classList.remove("open");
      cmdOverlay.setAttribute("aria-hidden", "true");
    }

    $("#cmdTrigger").addEventListener("click", openCmd);
    cmdOverlay.addEventListener("click", (e) => { if (e.target === cmdOverlay) closeCmd(); });
    cmdInput.addEventListener("input", () => renderCmdResults(cmdInput.value));
    cmdInput.addEventListener("keydown", (e) => {
      const items = cmdResults.querySelectorAll(".cmd-item");
      if (e.key === "ArrowDown") { e.preventDefault(); cmdSelected = Math.min(cmdSelected + 1, items.length - 1); items.forEach((el, i) => el.classList.toggle("selected", i === cmdSelected)); items[cmdSelected]?.scrollIntoView({ block: "nearest" }); }
      else if (e.key === "ArrowUp") { e.preventDefault(); cmdSelected = Math.max(cmdSelected - 1, 0); items.forEach((el, i) => el.classList.toggle("selected", i === cmdSelected)); items[cmdSelected]?.scrollIntoView({ block: "nearest" }); }
      else if (e.key === "Enter") { e.preventDefault(); items[cmdSelected]?.click(); }
      else if (e.key === "Escape") { closeCmd(); }
    });

    // 全局快捷键
    document.addEventListener("keydown", (e) => {
      if ((e.ctrlKey || e.metaKey) && e.key === "k") { e.preventDefault(); cmdOverlay.classList.contains("open") ? closeCmd() : openCmd(); }
      if (e.key === "Escape" && cmdOverlay.classList.contains("open")) closeCmd();
      // 数字键跳转
      if (!e.ctrlKey && !e.metaKey && !e.altKey && document.activeElement.tagName !== "INPUT" && document.activeElement.tagName !== "TEXTAREA") {
        const map = { "1": "#top", "2": "#resources", "4": "#community", "5": "#faq" };
        if (map[e.key]) { e.preventDefault(); document.querySelector(map[e.key])?.scrollIntoView({ behavior: "smooth" }); }
      }
    });

    // 深色/浅色主题切换
    const themeToggle = $("#themeToggle");
    const THEME_KEY = "angel-theme";
    const savedTheme = localStorage.getItem(THEME_KEY);
    if (savedTheme === "light") {
      document.body.classList.add("light-theme");
      themeToggle.setAttribute("aria-pressed", "true");
      themeToggle.setAttribute("aria-label", "切换深色主题");
    }
    themeToggle.addEventListener("click", () => {
      const isLight = document.body.classList.toggle("light-theme");
      themeToggle.setAttribute("aria-pressed", String(isLight));
      themeToggle.setAttribute("aria-label", isLight ? "切换深色主题" : "切换浅色主题");
      localStorage.setItem(THEME_KEY, isLight ? "light" : "dark");
    });

    // 主题色选择器
    const themeThemes = {
      purple: { "--purple": "#8b7cff", "--purple-2": "#c084fc", "--cyan": "#56e6c1", "--pink": "#ff7ab8" },
      ocean: { "--purple": "#3b82f6", "--purple-2": "#60a5fa", "--cyan": "#06b6d4", "--pink": "#0ea5e9" },
      sunset: { "--purple": "#f97316", "--purple-2": "#fb923c", "--cyan": "#fbbf24", "--pink": "#ec4899" },
    };
    const COLOR_KEY = "angel-color";
    const savedColor = localStorage.getItem(COLOR_KEY);
    if (savedColor && themeThemes[savedColor]) {
      Object.entries(themeThemes[savedColor]).forEach(([k, v]) => document.documentElement.style.setProperty(k, v));
      document.querySelectorAll(".swatch").forEach((s) => s.classList.toggle("active", s.dataset.theme === savedColor));
    }
    document.querySelectorAll(".swatch").forEach((sw) => {
      sw.addEventListener("click", () => {
        const theme = sw.dataset.theme;
        document.querySelectorAll(".swatch").forEach((s) => s.classList.remove("active"));
        sw.classList.add("active");
        if (themeThemes[theme]) {
          Object.entries(themeThemes[theme]).forEach(([k, v]) => document.documentElement.style.setProperty(k, v));
          localStorage.setItem(COLOR_KEY, theme);
        }
      });
    });

    // 字号调节
    const FONT_KEY = "angel-fontsize";
    const savedFont = localStorage.getItem(FONT_KEY) || "md";
    document.body.classList.add(`font-${savedFont}`);
    document.querySelectorAll(".font-btn").forEach((btn) => {
      if (btn.dataset.size === savedFont) btn.style.color = "white";
      btn.addEventListener("click", () => {
        const size = btn.dataset.size;
        document.body.classList.remove("font-sm", "font-md", "font-lg");
        document.body.classList.add(`font-${size}`);
        document.querySelectorAll(".font-btn").forEach((b) => b.style.color = "");
        btn.style.color = "white";
        localStorage.setItem(FONT_KEY, size);
      });
    });

    // 高对比模式
    const contrastToggle = $("#contrastToggle");
    const CONTRAST_KEY = "angel-contrast";
    if (localStorage.getItem(CONTRAST_KEY) === "on") {
      document.body.classList.add("high-contrast");
      contrastToggle.setAttribute("aria-pressed", "true");
    }
    contrastToggle.addEventListener("click", () => {
      const on = document.body.classList.toggle("high-contrast");
      contrastToggle.setAttribute("aria-pressed", String(on));
      localStorage.setItem(CONTRAST_KEY, on ? "on" : "off");
    });

    // 资源卡片也加入 reveal 动画

    // Service Worker 注册（离线缓存）
    if ("serviceWorker" in navigator) {
      const swCode = `
        const CACHE = "angel-v4";
        self.addEventListener("install", (e) => {
          e.waitUntil(caches.open(CACHE).then((c) => c.addAll(["./", "./angel-redesign-preview-v4.html"])).catch(() => {}));
          self.skipWaiting();
        });
        self.addEventListener("fetch", (e) => {
          if (e.request.method !== "GET") return;
          e.respondWith(
            caches.match(e.request).then((r) => r || fetch(e.request).then((res) => {
              const copy = res.clone();
              caches.open(CACHE).then((c) => c.put(e.request, copy)).catch(() => {});
              return res;
            }).catch(() => r))
          );
        });
        self.addEventListener("activate", (e) => { self.clients.claim(); });
      `;
      const blob = new Blob([swCode], { type: "application/javascript" });
      const swUrl = URL.createObjectURL(blob);
      navigator.serviceWorker.register(swUrl).catch(() => {});
    }
  </script>
</body>
</html>
